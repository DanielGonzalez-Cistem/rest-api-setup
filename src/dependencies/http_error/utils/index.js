//* Centralización de helpers
const { Exception } = require('./exception.error');
const { repositoryErrors } = require('./repository.errors');

module.exports = {
    Exception,
    repositoryErrors
}